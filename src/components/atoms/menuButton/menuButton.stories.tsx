import React from "react";
import MenuButton from "./MenuButton";

export const MenuBtn = () => <MenuButton dispatch={() => {}} />;

export default {
  title: "Atoms/Button",
  component: MenuButton,
};
